{{ post }}

<div class="post">

	<h3>{{ title }}</h3>
	<div class="date-post"><?php echo indonesian_date(date('Y-m-d H:i:s', strtotime($post[0]['created_on']))) ?></div>

	<div class="body">
		<img src="{{ thumb_detail }}" style="float:left; margin:7px 10px 5px 0;" />
		<span>{{ long_desc }}</span>
		<div class="cl"></div>
	</div>

</div>

{{ /post }}